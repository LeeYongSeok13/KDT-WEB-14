<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Practice Result</title>
  </head>
  <body>
    <h1><%= title %>폼 결과 확인하기</h1>
    <div><span><%= userInfo.name %></span>님 환영합니다!</div>
    <div>성별: <span><%= userInfo.gender %></span></div>
    <div>
      생년월일: <span><%= userInfo.year%></span>년
      <span><%= userInfo.month %></span>월 <span><%= userInfo.day %></span>일
    </div>
    <% if(userInfo.interest) {%>
    <div>취미: <span><%= userInfo.interest %></span></div>
    <% } %> <% if(userInfo.color) {%>
    <div>
      좋아하는 색상: <span><%= userInfo.color %></span>
      <input type="color" value="<%= userInfo.color %>" />
    </div>
    <% } %> <% if(userInfo.number) { %>
    <div>좋아하는 숫자: <span><%= userInfo.number %></span></div>
    <% } %>
  </body>
</html>
